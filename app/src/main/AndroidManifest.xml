<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />


    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="io.github.libxposed.permission.BIND_XPOSED_SERVICE" />

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- HyperOS/MIUI gates QUERY_ALL_PACKAGES behind a separate user permission. -->
    <uses-permission
        android:name="com.android.permission.GET_INSTALLED_APPS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".root.RootApplication"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:testOnly="false"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">

        <meta-data
            android:name="lyricon_module"
            android:value="true" />
        <meta-data
            android:name="lyricon_module_author"
            android:value="juren233, Tomakino" />
        <meta-data
            android:name="lyricon_module_description"
            android:value="@string/apple_music_provider_description" />
        <meta-data
            android:name="lyricon_module_tags"
            android:resource="@array/lyricon_module_tags" />



        <activity
            android:name=".ui.MainActivity"
            android:configChanges="locale|layoutDirection"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>

        <!-- 桌面入口别名（双 alias 互斥）：隐藏桌面图标 = 禁用 LauncherAlias 并启用
             LauncherEntry（无图标但保持 LAUNCHER 组件存在，LSPosed 模块详情的打开按钮始终可用） -->
        <activity-alias
            android:name=".ui.LauncherAlias"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <receiver
            android:name=".ui.LauncherIconStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- hook（SystemUI）上报「歌词/翻译实际来源」：LSPosed 远程偏好只有 App→hook 单向可靠，
             hook 侧写入不落盘，所以改用显式广播，由本 Receiver 落到 App 本地偏好。
             exported 才能接收来自 SystemUI 进程的广播（仅影响只读诊断展示）。 -->
        <receiver
            android:name=".service.LyricOriginReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.genius.hyperlyrics.action.LYRIC_ORIGIN_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 备用入口：隐藏桌面图标时启用，图标为透明占位（视觉隐藏但 LAUNCHER 组件
             始终存在，LSPosed 模块详情右下角打开按钮始终可用） -->
        <activity-alias
            android:name=".ui.LauncherEntry"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher_transparent"
            android:targetActivity=".ui.MainActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".service.LiveLyricService"
            android:label="@string/label_lyric_listener_service"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name=".service.ClassicAodFocusRefreshProvider"
            android:authorities="${applicationId}.classic_aod_focus_refresh"
            android:exported="true"
            android:multiprocess="false" />

        <service
            android:name=".service.LyricTileService"
            android:icon="@drawable/lyrictile"
            android:label="@string/label_lyric_enable_service"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
